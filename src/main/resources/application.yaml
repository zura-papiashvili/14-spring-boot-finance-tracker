spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_tracker_db
    username:
    password:
    driver-class-name: com.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
